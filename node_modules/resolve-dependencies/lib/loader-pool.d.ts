import { File, FileMap } from './file';
import { ResolveDepOptions } from './options';
export declare class Loader {
    private options;
    private pool;
    private workerOptions;
    private size;
    private currentWorker;
    private initializing;
    constructor(options: ResolveDepOptions);
    initialize(): Promise<any>;
    quit(): void;
    private getWorker();
    loadEntry(wd: string, request: string, files?: FileMap): Promise<{
        entry: File | null;
        files: FileMap;
    }>;
    private load(cd, request, files?);
}
